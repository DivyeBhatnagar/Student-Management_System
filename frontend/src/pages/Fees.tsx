import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/Card';

const FeesPage = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Fees</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the fees page.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default FeesPage;
