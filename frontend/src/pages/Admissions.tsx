import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/Card';

const AdmissionsPage = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Admissions</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the admissions page.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default AdmissionsPage;
