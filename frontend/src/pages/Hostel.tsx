import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/Card';

const HostelPage = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Hostel</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the hostel page.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default HostelPage;
