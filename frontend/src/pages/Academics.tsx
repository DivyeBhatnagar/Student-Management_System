import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/Card';

const AcademicsPage = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Academics</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the academics page.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default AcademicsPage;
